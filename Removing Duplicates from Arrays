const arr = [1,1,6,2,3,3,3,8,4,5,6,6,7,8,8,9,1,2,3];

// Process 1: Using forEach & includes
// const newArr =[];

// arr.forEach((item)=>{
//     if(!newArr.includes(item)){
//         newArr.push(item);
//     }
// })

// console.log(newArr);

// Process 2: Using filter() & indexOf()

const newArray= arr.filter((item,index)=>arr.indexOf(item)===index);
console.log(newArray);
